server {
	listen 80;
	server_name localhost outdoors;
	passenger_enabled on;
	root /var/www/gamocosm/public;
	location ~* ^/assets/ {
		expires 1y;
		add_header Cache-Control public;
		add_header Last-Modified "";
		add_header ETag "";
		break;
	}
}
